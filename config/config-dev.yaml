app:
  name: "Sample Rest Application in Python"

datastore:
  sqllite:
    url: "/data/jobs.db"
  postgres:
    host_name: localhost
    port: 5432
    user: postgres
    password: ENV_POSTGRES_PW
    database: jobs_db
